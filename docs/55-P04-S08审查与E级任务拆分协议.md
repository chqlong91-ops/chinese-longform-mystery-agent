# P04-S08 审查与 E 级任务拆分协议

## 1. 任务身份

- task：`P04-S08：运行 project-bound L6-P、强制失败与选择性恢复`
- predecessor：`P04-S06-FROZEN-BASELINE@v001`、`S07-BL-LOSTBOX@v002` 与 `P04-S07 → P04-S08 handoff v002`
- production evidence：`P04-AC08`、`P04-AC09`、`P04-AC10`
- final acceptance owner：`P04-S10`
- current action：审查并拆分 E01—E10；不运行 L6-P，不读取小说正文，不授予 P04 最终结论

## 2. 审查结论与调整

原目标成立，但必须收紧为“恢复与失败控制任务”，不能吞并 S09 的跨项目泛化/体验审计或 S10 的阶段终裁。

1. S08 的真实恢复点固定为三个：深度试点中段、深度试点终态、广度试点代表切片；不得用 synthetic `L6-S`、机械 schema 校验或当前会话复述代替。
2. 三次 `L6-P` 必须使用相互隔离的 clean session，严格按各自 Recovery Manifest v2 的声明顺序读取；主控制器不向恢复上下文补发旧聊天、小说正文或先验答案。
3. 真实项目载荷进入独立上下文仍是 Tier 3 例外，但三次恢复应合并为一个 object-bound 批量例外停点。其余 preflight、失败注入、选择性恢复、状态验证和报告均为 Tier 1，自主执行，不形成授权循环。
4. 深度中段恢复评价“历史 checkpoint 是否可被精确恢复”，不得把中段状态误写成项目 current；深度终态和广度切片分别按其 package-bound epoch 评价。
5. 强制失败覆盖 source/hash drift、依赖缺失、状态冲突、部分写入、越界读取、错误确认继承、跨项目污染和 non-unique next；失败必须零状态授予、零项目载荷写入。
6. 选择性恢复只重跑受影响的 package/registry/view scope；不得通过重跑整部小说、读取全稿或覆盖 protected source 证明恢复成功。
7. S08 只生产 AC08/09/10 的 production evidence 和 S09 handoff。它不评价 AC06/07/11，不作 P04 12 项最终裁决，不创建 P04 frozen baseline。

## 3. 输入与读取边界

控制器默认只读取 Agent canonical registry、P04 合同、S06/S07 baseline/handoff、三个 RMv2 manifest、project registry 的结构状态和既有遥测。小说正文、ASM payload、candidate/formal/protected source、C10 和其他小说均不在控制器读取集。

独立 clean session 只读取 manifest 声明的 `project_payload_reads` 与 `environment_reads`，并记录 actual reads、not-read、over-read、writes、hash mismatch、assertion result、context disposal 和 unique next。任何未声明读取即 fail-closed。

## 4. E 级任务

`P04-S08-E01`：冻结 S08 输入 exact-set、三个恢复点、证据等级、权限、读写预算、停止条件与非目标。

`P04-S08-E02`：对三个 Recovery Manifest v2 执行 schema、identity、hash、dependency、load graph、budget、assertion 和 package exact-set preflight；只允许建立控制层 successor，不运行 L6-P。

`P04-S08-E03`：建立统一 L6-P batch run contract、clean-session independence contract、read ledger 和 receipt schema；生成三次真实载荷独立恢复所需的唯一 Tier 3 批量例外停点。

`P04-S08-E04`：在独立 clean session 中运行深度试点中段 `L6-P`，核验历史 checkpoint、已确认范围、未完成 Act III、读边界和唯一 checkpoint-next。

`P04-S08-E05`：在新的独立 clean session 中运行深度试点终态 `L6-P`，核验 current ASM/全书状态、确认/交付边界、residual nonclaims 和唯一下一步。

`P04-S08-E06`：在新的独立 clean session 中运行广度试点切片 `L6-P`，核验 12 章 planned-only 与 CH04—CH07 formal 范围分离、两次内容停点、无 full-book 外推和唯一下一步。

`P04-S08-E07`：运行八类隔离强制失败矩阵，证明 drift、缺依赖、冲突、partial write、over-read、错误确认继承、跨项目污染和 non-unique next 均 fail-closed、零状态授予。

`P04-S08-E08`：对 E07 失败运行最小修复与选择性重跑，验证原子事务、single current、deterministic view rebuild、partial-write=0、项目载荷零污染和未触及结论保留。

`P04-S08-E09`：聚合三份 L6-P receipt、失败/恢复证据和运行遥测，建立 AC08/09/10 production-evidence matrix，检查同一恢复 runtime 无项目专用分叉、无内部授权循环、无双 current。

`P04-S08-E10`：评价 S08 task gate，冻结 S08 baseline、proved/unproved/risk/nonclaims 与 S08→S09 handoff，并把唯一下一步切换为 `review and split P04-S09`。

## 5. 顺序与停点

执行顺序固定为：

`E01 -> E02 -> E03 -> E04 -> E05 -> E06 -> E07 -> E08 -> E09 -> E10`

E01—E03 的内部设计、校验和对象绑定均为 Tier 1。若 E03 的 preflight 全部通过，在首次把真实项目 package payload 送入独立上下文前形成一次 Tier 3 批量例外停点；该停点同时精确绑定 E04—E06 三个 package，不得拆成逐文件、逐 hash 或逐恢复点授权。获准后 E04—E10 自动连续执行，除非 fail-closed。

S08 没有 Tier 2 创意/正文确认点，也不修改任何小说内容。

## 6. 验收条件

S08 只有同时满足以下条件才能通过自身 task gate：

1. 三个 package 均通过 identity/schema/hash/dependency/exact-set preflight；
2. 深度中段、深度终态、广度切片各有一份独立 `L6-P` receipt；
3. 三次运行 actual reads 可复算、over-read=0、writes=0、assertions 全部匹配、context disposed；
4. 中段历史 checkpoint、终态 current epoch 与广度切片 range ceiling 没有混同；
5. 八类强制失败全部 fail-closed，state grants=0，项目载荷污染=0；
6. 选择性重跑只覆盖可达受影响范围，未触及对象与结论保持原状态；
7. Agent 和两个 project registry 均 double-current=0，派生视图可确定性重建，partial-write=0；
8. 同一 recovery runtime/receipt schema 服务三个恢复点，不存在项目专用协议分叉；
9. internal technical authorization requests=0，真实载荷 Tier 3 停点不超过一次；
10. AC08/09/10 只形成 production evidence，final verdict 仍为 `not-evaluated / owner P04-S10`；
11. S09、S10 未执行，P04 frozen baseline 不存在；
12. S08→S09 handoff 的 proved/unproved、risk、nonclaims 和 unique next 唯一。

## 7. 失败关闭

任一 manifest/hash/dependency 漂移、package 资源缺失、clean-session 独立性失败、越界读取、写入尝试、断言冲突、双 current、状态污染、选择性重跑扩大到小说载荷或 unique next 不唯一时，立即停止。不得签发对应 L6-P receipt，不得用用户授权绕过技术阻塞，只记录最小补救、owner 和 checkpoint。

## 8. 本轮边界

本轮仅完成审查与 E01—E10 拆分，执行进度为 `0/10`。没有启动独立 clean session，没有读取或修改小说正文，没有创建 L6-P receipt，没有运行强制失败或选择性恢复，没有请求 Tier 3 例外，也没有授予 P04 AC 最终通过或 frozen baseline。

## 9. 唯一下一步

`execute P04-S08-E01`。
