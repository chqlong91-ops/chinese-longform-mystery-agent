# P04-S01 审查与 E 级任务拆分协议

## 1. 任务身份

- 父阶段：`P04：可执行创作编排与跨项目泛化验证`
- S 任务：`P04-S01：冻结 P04 阶段契约、P03-R1 交接与验收追踪`
- 前置：P04 definition 已确认；P04-S01 已规划但尚未执行；`P03-R1-FROZEN-BASELINE@v002` 有效
- 直接验收责任：`P04-AC01`、`P04-AC12`
- 追踪责任：为 `P04-AC02—P04-AC11` 冻结 requirement、owner、evidence 和终裁入口
- 当前动作：只完成 E 级拆分，不执行 E01—E10

## 2. 审查结论与调整

P04-S01 的原目标成立，但必须作以下收敛：

1. S01 只冻结 P04 可以怎样执行，不实现初始化器、编排器、遥测或试点运行；这些能力分别归 S02—S09。
2. 明确区分阶段定义已确认、父任务已拆分、S01 已拆分、S01 已执行、试点已准入和阶段已验收，禁止用一个“完成”状态互相推断。
3. 在选择故事种子之前，先冻结双试点的证据角色：深度试点证明完整成书闭环，广度试点证明异质项目迁移和代表性跨章切片；C10 仅为冻结历史参照。
4. `L6-S` 与 project-bound `L6-P` 保持分离。S01 只冻结 L6-P 的 Tier 3 边界、证据要求和停止条件，不运行任何 L6-P。
5. 内部 permit、lease、hash、A0—A5 和普通门禁归一级自主事务，不得重建用户逐动作授权循环。
6. S01 文档不能循环自证 S01 通过。E09 必须运行实际契约/失败/追踪验证，E10 才能作 S01 acceptance 和 handoff。

## 3. E 级任务

`P04-S01-E01`：冻结 S01 输入 exact-set、来源优先级、P03-R1 baseline 与 P04 定义指纹。

`P04-S01-E02`：冻结 P04 术语、阶段身份、父/S/E 层级与状态语义。

`P04-S01-E03`：冻结 P04 需求、目标、非目标、coverage 与兼容边界。

`P04-S01-E04`：建立 P03-R1→P04 disposition matrix 与能力交接。

`P04-S01-E05`：冻结深度/广度双试点证据角色、异质性约束与准入前置。

`P04-S01-E06`：冻结 P04-AC01—AC12、owner、双向追踪和 verdict 语义。

`P04-S01-E07`：冻结 evidence/read/write/context budget 与证据等级上限。

`P04-S01-E08`：冻结三级权限停点、生效时序、stop/fail-closed/no-progress/recovery 规则。

`P04-S01-E09`：运行契约场景、强制失败、trace 完整性、链接/schema/回归验证。

`P04-S01-E10`：冻结正式 P04 stage contract、S01 acceptance 与 S01→S02 handoff。

## 4. 顺序与依赖

执行顺序固定为：

`E01 -> E02 -> E03 -> E04 -> E05 -> E06 -> E07 -> E08 -> E09 -> E10`

每个 E 任务只消费已完成前项的 actual 输出。任何输入漂移、双 current、追踪缺口、循环自证、证据越级或非唯一下一步必须 fail-closed。

## 5. S01 预期产物

1. P04 输入 exact-set 与来源优先级记录；
2. 正式 P04 stage contract；
3. P03-R1→P04 handoff/disposition matrix；
4. P04 requirement、AC、owner、evidence 双向追踪矩阵；
5. 双试点证据角色与异质性/准入前置；
6. evidence/read/write/context budget；
7. 三级停点、失败关闭、无进展和恢复契约；
8. 契约场景、强制失败和回归验证结果；
9. S01 acceptance；
10. S01→S02 handoff。

## 6. 验收条件

S01 只有在 E01—E10 全部执行且满足以下条件时才可完成：

1. P04 目标、非目标、coverage、兼容边界和 source-of-truth 唯一；
2. P03-R1 的每项能力均有 adopt、adapt、replace、reference-only 或 out-of-scope disposition；
3. P04-AC01—AC12 均有生产 owner、终裁 owner、所需证据和 nonclaim；
4. 双试点证据角色和异质性标准先于具体种子冻结；
5. Tier 1/Tier 2/Tier 3 停点没有内部授权循环；
6. novel/project read-write budget 在 S05 准入前为 0/0；
7. 实际验证通过且不存在循环自证、追踪孤儿、状态层级混同或非唯一下一步；
8. S01→S02 handoff 只授予初始化器实现入口，不授予试点准入、小说状态或 L6-P。

## 7. 当前边界与唯一下一步

本轮已完成 E01—E10 的任务拆分，但执行进度仍为 `0/10`。没有选择或准入试点，没有读取或修改小说，没有运行 L6-P，也没有创建 P04 frozen baseline。

唯一下一步：执行 `P04-S01-E01`。
