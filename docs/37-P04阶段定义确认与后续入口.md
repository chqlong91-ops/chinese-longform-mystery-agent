# P04 阶段定义确认与后续入口

## 1. 确认对象

- definition object：`P04-DEFINITION-CANDIDATE@v001`
- definition source：`docs/36-P04阶段定义审查与候选契约.md`
- stage id：`P04`
- stage name：`可执行创作编排与跨项目泛化验证`
- confirmation：`user-confirmed / exact-definition-bound`

用户已明确确认：

> 确认 P04：可执行创作编排与跨项目泛化验证 的阶段定义。

## 2. 确认效果

本次确认把 P04 候选定义提升为已确认的阶段定义。以下内容随定义一并确认：

1. P04 是 P03 和 P03-R1 之后的下一主阶段，不是 `P03-S11` 或 `P03-R1-S09`。
2. P04 聚焦可执行创作编排、低交互真实运行、两个异质新试点、project-bound `L6-P` 和跨项目泛化证据。
3. 验证采用一个端到端深度试点和一个代表性广度试点；C10 只作冻结历史参照，不计入两个新试点。
4. `P04-AC01—AC12` 是阶段候选验收标准，全部 `yes/evaluated` 才允许创建 P04 frozen baseline。
5. 内部 permit、lease、hash、A0—A5 和普通门禁不再形成用户授权循环。
6. Tier 2 内容确认和 Tier 3 核心创意、真实项目 L6、外部或不可逆动作边界继续有效。

## 3. 不包含事项

本次确认不产生以下效果：

1. 不启动 P04 执行；
2. 不拆分 P04-S01 或其他子任务；
3. 不选择、创建或准入任何新小说项目；
4. 不读取或修改 C10、其他小说、正文、candidate、formal 或 ASM；
5. 不追溯改变 P03 的 `9/11 satisfied / not-passed`；
6. 不授予 project-bound `L6-P`、外部发布或 Tier 3 创意决定。

## 4. 当前状态

- P04 definition review：`complete`
- P04 definition confirmation：`yes/evaluated`
- P04 definition sync：`synced`
- P04 execution lifecycle：`not-started`
- P04 task split：`not-started`
- pilot admission：`not-started`

## 5. 唯一下一步

审查并拆分 `P04-S01`。该动作只建立 P04 第一项子任务及其可执行子任务；P04-S01 的实际执行仍应由后续明确指令触发。

