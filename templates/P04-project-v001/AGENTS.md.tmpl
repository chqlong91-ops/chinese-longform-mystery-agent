# {{DISPLAY_NAME}} 创作工作区

本目录由 `P04-PROJECT-TEMPLATE@v001` 初始化。`04-state/project-registry.json` 是本项目唯一机器可读状态源；Markdown 视图只读、可重建，不能授予 current、确认、同步或门禁。

启动顺序：读取本文件、`00-workspace-guide/RECOVERY.md`、project registry、当前任务近场证据。未到内容确认或真正例外动作时，按三级权限机制自主推进。
