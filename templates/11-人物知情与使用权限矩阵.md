# 人物知情与使用权限矩阵

## 产物契约

- 用途：限制人物观察、解释、披露、引用和公开事实的权限。
- 创建路由：`R-TRANSFORM`、`R-AUDIT`。
- 更新触发：事实来源、取得时间、理解能力或使用权限变化。
- 状态记录：项目状态表；未决权限标为 `Open` 或 `Blocking`。
- 真相源：真实时间线、来源载体、现实能力清单和明确确认。

| 事实/来源 | 人物 | 何时如何观察或取得 | 何时能解释 | 可向谁披露 | 可在哪种程序引用 | 可否公开 | 确定度 | 读者何时知道 |
|---|---|---|---|---|---|---|---|---|

确定度使用：`none`、`suspects`、`infers`、`knows`。

## P02-S08 同步证据

| SFD item | Person/fact/time | Observe/obtain old->new | Interpret old->new | Disclose/use old->new | CFD/A4 | SYR/SYV | Result |
|---|---|---|---|---|---|---|---|
| `<ref>` | `<refs>` | `<values>` | `<values>` | `<values>` | `<refs>` | `<refs>` | `matched|partial|blocked|stale|conflict` |

用户确认不能把 Agent/读者知识变成人物知识，也不能自动授予 disclose、cite/file、publicize 或 institution-accept 权限。

## P02-S09 兼容入口

- 本对象的 stable ID、version、scope、currentness、fingerprint 与引用关系可作为 IGS 节点/边证据；路径或摘要不是身份。
- 本对象变化时，只沿显式 relation 和适用 dimension 形成 affected item；未触及范围必须有 PEI，不能全量清空。
- 若被列入幕边界或恢复白名单，必须引用 current BSS/RPM 条目及稳定位置；本模板自身不证明 propagation、act-boundary、recovery 或 final 通过。
